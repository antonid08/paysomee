<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Application.Toolbar.Default" parent="@style/ThemeOverlay.AppCompat.ActionBar">
        <item name="android:elevation">@dimen/default_elevation</item>
        <item name="android:background">@color/colorAccent</item>
    </style>

    <style name="Application.Button" parent="@android:style/Widget.Button">
        <item name="android:textAllCaps">true</item>
    </style>

    <style name="Application.Label.Primary" parent="@android:style/Widget.TextView">
        <item name="android:textSize">@dimen/text_size_normal</item>
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:gravity">center_vertical</item>
    </style>

</resources>
